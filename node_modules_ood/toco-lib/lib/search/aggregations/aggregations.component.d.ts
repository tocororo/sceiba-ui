import { EventEmitter, OnInit } from "@angular/core";
import { Aggr, AggrBucket } from "../../entities/public-api";
export interface AggregationsSelection {
    [id: string]: string[];
}
export declare class AggregationsComponent implements OnInit {
    aggregations: {
        [id: string]: Aggr;
    };
    /***
     * {
     *  'country': ['Cuba','Peru'],
     *  'state': ['New York']
     *  ...
     * }
     *
     * in the aggregation country, buckets 'Cuba' and 'Peru' are selected
     * in the aggregation state, bucket 'New York' is selected
     *
     */
    selectedAggr: AggregationsSelection;
    keySelect: EventEmitter<AggregationsSelection>;
    keys: any[];
    constructor();
    ngOnInit(): void;
    private _translate;
    isSelected(aggrKey: any, bucket: AggrBucket): boolean;
    selectionChange(aggrKey: any, bucket: AggrBucket): void;
}
