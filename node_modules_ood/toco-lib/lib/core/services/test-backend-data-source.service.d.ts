import { Observable } from "rxjs";
import { PageRequest, Page } from './backend-data-source';
interface User {
    id: number;
    name: string;
    username: string;
    email: string;
    address: {
        street: string;
        suite: string;
        city: string;
        zipcode: string;
        geo: {
            lat: string;
            lng: string;
        };
    };
    phone: string;
    website: string;
    company: {
        name: string;
        catchPhrase: string;
        bs: string;
    };
    registrationDate: Date;
}
export interface UserQuery {
    search: string;
    registration: Date;
}
export declare class UserService {
    page(pageRequest: PageRequest): Observable<Page<User>>;
    private users;
}
export {};
