import { MatSnackBar } from '@angular/material/snack-bar';
import { MatDialog } from '@angular/material/dialog';
export declare enum HandlerComponent {
    snackBar = "snackBar",
    dialog = "dialog"
}
export declare enum StatusCode {
    OK = 200,
    serverError = 500,
    notFound = 400
}
/**
 * Object that is used with the `DialogContentComponent` component.
 * It contains the different data that are showed in the message.
 */
export declare class DialogMessageData {
    /**
     * Message title.
     */
    title: string;
    /**
     * Message content.
     */
    content: string;
}
/**
 * Object that is used with the `DialogDeleteConfirmComponent` component.
 * It contains the different data that are showed in the message.
 */
export declare class DialogDeleteConfirmMessageData {
    /**
     * Article of the type to delete.
     */
    delTypeArt: string;
    /**
     * Type to delete.
     */
    delType: string;
    /**
     * Value to delete.
     */
    delValue: string;
}
/**
 * Simple dialog message.
 */
export declare class DialogContentComponent {
    data: DialogMessageData;
    constructor(data: DialogMessageData);
}
/**
 * Dialog confirm message used to delete something.
 */
export declare class DialogDeleteConfirmComponent {
    data: DialogDeleteConfirmMessageData;
    constructor(data: DialogDeleteConfirmMessageData);
}
export declare class MessageHandler {
    private _snackBar?;
    dialog?: MatDialog;
    constructor(_snackBar?: MatSnackBar, dialog?: MatDialog);
    showMessage(status: StatusCode, message?: string, component?: HandlerComponent, title?: string, width?: string): void;
    private componentHandler;
}
