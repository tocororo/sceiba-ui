import { OnInit } from '@angular/core';
import { Router, ActivatedRoute } from '@angular/router';
import { OrganizationService } from '../../backend/public-api';
import { TableContent, ContainerContent } from '../../forms/public-api';
export declare class OrgTableEditComponent implements OnInit {
    private _router;
    private _activatedRoute;
    private _organizationService;
    /**
     * Contains the content of the identifiers section.
     */
    identifiersContent: ContainerContent;
    /**
     * The search filter.
     */
    /**
     * The sources list.
     * Use this field to initialize only; to change value use the `_tableControl` field.
     */
    tableContent: TableContent<any>;
    private _tableControl;
    constructor(_router: Router, _activatedRoute: ActivatedRoute, _organizationService: OrganizationService);
    ngOnInit(): void;
    private _initSearchContent;
    private _initTableContent;
    private _getMySources;
    doOperation(): void;
}
