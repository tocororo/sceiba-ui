import { Address } from '../../../entities/public-api';
export declare class OrgViewAddressComponent {
    /**
     * An address associated with the organization.
     * By default, its value is `undefined`.
     */
    value: Address;
    constructor();
}
