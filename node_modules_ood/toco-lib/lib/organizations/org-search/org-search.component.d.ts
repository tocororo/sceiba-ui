import { OnInit, EventEmitter } from '@angular/core';
import { Organization, HitList } from '../../entities/public-api';
import { FormControl } from '@angular/forms';
import { OrganizationServiceNoAuth } from '../../backend/public-api';
import { HttpParams } from '@angular/common/http';
export declare class OrgSearchComponent implements OnInit {
    private _orgService;
    /**
     * Input `orgCtrl` is a FormControl
     */
    orgCtrl: FormControl;
    filteredOrg: HitList<Organization>;
    params: HttpParams;
    /**
     * Input `orgFilter` is a dict with `type` and `value` to filter the organizations,
     * @Example { type: 'country' , value: 'Cuba" }
     */
    orgFilter: {
        type: string;
        value: string;
    } | Array<{
        type: string;
        value: string;
    }>;
    placeholder: string;
    label: string;
    appearance: string;
    /**
     * Input `cleaning` is a boolen, if true then clean the search
     */
    cleaning: boolean;
    selectedOrg: EventEmitter<Organization>;
    toSearch: number;
    constructor(_orgService: OrganizationServiceNoAuth);
    ngOnInit(): void;
    displayFn(org?: Organization): string | undefined;
}
