import { OnInit } from '@angular/core';
import { MatTableDataSource, MatPaginator, MatSnackBar } from '@angular/material';
import { NotificationService } from '../../backend/public-api';
import { NotificationInfo } from '../notification-button/notification-button.component';
export declare class NotificationListComponent implements OnInit {
    private service;
    private _snackBar;
    dataSource: MatTableDataSource<NotificationInfo>;
    columnsToDisplay: string[];
    columnsLabels: string[];
    expandedElement: Notification;
    moment: any;
    pageSizeOptions: number[];
    paginator: MatPaginator;
    constructor(service: NotificationService, _snackBar: MatSnackBar);
    ngOnInit(): void;
    getNotificationsListData(): void;
    setnotificationViewed(id: number): void;
}
