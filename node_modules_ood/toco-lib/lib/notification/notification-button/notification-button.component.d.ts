import { OnInit } from '@angular/core';
import { MatSnackBar } from '@angular/material';
import { NotificationService } from '../../backend/public-api';
import { OAuthStorage } from 'angular-oauth2-oidc';
export declare class NotificationButtonComponent implements OnInit {
    private service;
    private _snackBar;
    private oauthStorage;
    color: string;
    buttonType: string;
    count: number;
    notifications: Array<NotificationInfo>;
    moment: any;
    private timerSuscription;
    private timerObserver;
    constructor(service: NotificationService, _snackBar: MatSnackBar, oauthStorage: OAuthStorage);
    ngOnInit(): void;
    notificationsCount(): number | "" | "+10";
}
declare class UserNotification {
    id: string;
    name: string;
    lastname: string;
    email: string;
}
export declare class NotificationInfo {
    classification: {
        label: string;
        color: string;
    };
    receiver_id: UserNotification;
    viewed: boolean;
    viewed_date: string;
    description: string;
    createdAt: string;
    emiter: UserNotification;
    app: string;
    data: {};
}
export {};
