import { OnInit } from '@angular/core';
import { FormControl } from '@angular/forms';
import { Observable } from 'rxjs';
import { InputControl } from '../../input/input.control';
import { Term } from '../../../entities/public-api';
export declare class TermParentComponent extends InputControl implements OnInit {
    formControl: FormControl;
    inputId: string;
    filteredOptions: Observable<Term[]>;
    selectOptions: Term[];
    currentTerm: Term;
    parentTerm: Term;
    constructor();
    ngOnInit(): void;
    private _updateFilteredOptions;
    private _get_terms;
    addParent(value: Term): void;
    removeParent(): void;
    private setValueToInternalControl;
}
