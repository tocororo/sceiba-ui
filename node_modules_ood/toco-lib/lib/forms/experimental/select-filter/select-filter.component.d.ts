import { OnInit } from "@angular/core";
import { FormControl } from "@angular/forms";
import { Observable } from "rxjs";
import { InputControl } from '../../input/input.control';
import { SelectOption } from "../../input/select/select-input.component";
interface SelectFilterComponentExtraContent {
    multiple: boolean;
    selectedTermsIds: [];
    excludeTermsIds: [];
}
export declare class SelectFilterComponent extends InputControl implements OnInit {
    chipsFormControl: FormControl;
    inputId: string;
    filteredOptions: Observable<SelectOption[]>;
    chipsList: SelectOption[];
    selectOptions: SelectOption[];
    terms: SelectOption[];
    loading: boolean;
    extraContent: SelectFilterComponentExtraContent;
    searchText: string;
    constructor();
    ngOnInit(): void;
    private selectOptionsLoaded;
    private setValidation;
    private addTermToValue;
    private removeTermFromValue;
    private _updateFilteredOptions;
    addChips(value: SelectOption): void;
    removeChip(index: number): void;
}
export {};
