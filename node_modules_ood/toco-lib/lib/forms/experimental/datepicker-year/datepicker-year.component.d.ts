import { InputControl } from '../../input/input.control';
import { OnInit } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
import { MatDatepicker } from '@angular/material';
import { Moment } from 'moment';
export declare const YEAR_MODE_FORMATS: {
    parse: {
        dateInput: string;
    };
    display: {
        dateInput: string;
        monthYearLabel: string;
        dateA11yLabel: string;
        monthYearA11yLabel: string;
    };
};
export declare class DatepickerYearComponent extends InputControl implements OnInit, ControlValueAccessor {
    /** Component label */
    label: string;
    _max: Moment;
    max: number | Date;
    _min: Moment;
    min: number | Date;
    touchUi: boolean;
    _picker: MatDatepicker<Moment>;
    onChange: (year: Date) => void;
    onTouched: () => void;
    ngOnInit(): void;
    constructor();
    writeValue(date: Date): void;
    registerOnChange(fn: any): void;
    registerOnTouched(fn: any): void;
    setDisabledState(isDisabled: boolean): void;
    _yearSelectedHandler(chosenDate: Moment, datepicker: MatDatepicker<Moment>): void;
    _openDatepickerOnClick(datepicker: MatDatepicker<Moment>): void;
    _openDatepickerOnFocus(datepicker: MatDatepicker<Moment>): void;
    /** Whether the given year is enabled. */
    private _isYearEnabled;
}
