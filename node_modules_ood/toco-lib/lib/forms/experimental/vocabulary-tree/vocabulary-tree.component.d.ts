import { OnInit } from "@angular/core";
import { FormControl } from "@angular/forms";
import { Observable } from "rxjs";
import { TaxonomyService } from '../../../backend/public-api';
import { VocabulariesInmutableNames, TermNode, Term } from '../../../entities/public-api';
import { InputControl } from '../../input/input.control';
interface VocabularyComponentExtraContent {
    multiple: boolean;
    selectedTermsIds: [];
    excludeTermsIds: [];
    level: number;
    vocab: VocabulariesInmutableNames;
}
/**
 * A control to select a term or terms in a vocabulary.
 */
export declare class VocabularyTreeComponent extends InputControl implements OnInit {
    private service;
    chipsFormControl: FormControl;
    levelsOptions: Array<TermNode[]>;
    levelsSelection: Array<Term>;
    lastLevelTerm: Term;
    inputId: string;
    filteredOptions: Observable<TermNode[]>;
    chipsList: TermNode[];
    leafsOptions: TermNode[];
    terms: TermNode[];
    loading: boolean;
    extraContent: VocabularyComponentExtraContent;
    searchText: string;
    constructor(service: TaxonomyService);
    ngOnInit(): void;
    private _setLevelsOptions;
    onSelectionChange(level: any, item: Term): void;
    private setValidation;
    private addTermToValue;
    private removeTermFromValue;
    private _updateFilteredOptions;
    private _get_terms;
    addChips(value: TermNode): void;
    removeChip(index: number): void;
    getTermNameInATree(node: TermNode): any;
}
export {};
