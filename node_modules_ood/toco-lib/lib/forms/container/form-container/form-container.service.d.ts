import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { Environment } from '../../../core/env';
import { Response } from '../../../core/public-api';
/**
 * This service is deprecated.
 */
export declare class FormContainerService {
    private httpClient;
    private env;
    private httpOptions;
    constructor(httpClient: HttpClient, env: Environment);
    /**
     * Sends data to the server.
     * @param endPoint The data end point. For example, `/add_new`.
     * @param token The autorization token.
     * @param data The data to send, must be parsed.
     */
    sendPostData(endPoint: string, token: string, data: any): Observable<Response<any>>;
}
