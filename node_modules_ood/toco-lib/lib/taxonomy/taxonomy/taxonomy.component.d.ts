import { OnDestroy, OnInit } from '@angular/core';
import { Vocabulary } from '../../entities/public-api';
export declare class TaxonomyComponent implements OnInit, OnDestroy {
    vocabs: {
        title: string;
        cols: number;
        rows: number;
    };
    terms: {
        title: string;
        cols: number;
        rows: number;
    };
    current_vocab: {
        name: string;
        description: string;
    };
    currentVocab: Vocabulary;
    constructor();
    ngOnInit(): void;
    vocabChange(vocab: Vocabulary): void;
    ngOnDestroy(): void;
}
