import { OnInit } from '@angular/core';
import { TaxonomyService } from '../../backend/public-api';
import { Term } from '../../entities/public-api';
export declare class TermsViewerComponent implements OnInit {
    private taxonomyService;
    /**
     * receive vocab_id and a list of terms ids.
     * show in a chiplist the terms of vocab_id
     */
    vocabulary_id: any;
    ids: number[];
    terms: Term[];
    constructor(taxonomyService: TaxonomyService);
    ngOnInit(): void;
}
