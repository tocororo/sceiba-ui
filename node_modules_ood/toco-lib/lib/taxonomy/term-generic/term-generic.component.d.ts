import { OnInit } from "@angular/core";
import { PanelContent_Depr, FormContainerAction } from "../../forms/public-api";
import { Vocabulary } from "../../entities/public-api";
import { FormBuilder, FormGroup } from "@angular/forms";
import { MatSnackBar } from "@angular/material";
export declare class TermGenericComponent implements OnInit {
    private _formBuilder;
    _snackBar: MatSnackBar;
    private data;
    panels: PanelContent_Depr[];
    formGroup: FormGroup;
    action: FormContainerAction;
    actionLabel: string;
    hasService: boolean;
    accept: any;
    vocab: Vocabulary;
    constructor(_formBuilder: FormBuilder, _snackBar: MatSnackBar, data: any);
    ngOnInit(): void;
    /**
     * Returns the panel's content.
     */
    private _getContent;
    private _getTermDataObject;
}
