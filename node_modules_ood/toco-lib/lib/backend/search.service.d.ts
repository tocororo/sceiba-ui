import { HttpBackend, HttpClient, HttpParams } from '@angular/common/http';
import { Observable } from 'rxjs';
import { Environment, Response } from '../core/public-api';
import { Organization, Record, SearchResponse, Source } from '../entities/public-api';
export declare class SearchService {
    private env;
    private handler;
    private prefix;
    http: HttpClient;
    constructor(env: Environment, handler: HttpBackend);
    getAggregation(field: any, size?: number): Observable<Response<any>>;
    getRecords(params: HttpParams): Observable<SearchResponse<Record>>;
    getSources(params: HttpParams): Observable<SearchResponse<Source>>;
    getOrganizations(params: HttpParams): Observable<SearchResponse<Organization>>;
    getOrganizationById(id: string): Observable<SearchResponse<Organization>>;
    updateOrganizations(data: Organization): Observable<Organization>;
    getSourcesOrgAggregation(uuid: any): Observable<Response<any>>;
}
