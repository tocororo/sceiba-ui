import { OnInit } from '@angular/core';
import { MatPaginator, PageEvent } from '@angular/material/paginator';
import { MatTableDataSource } from '@angular/material/table';
import { MetadataService } from '../../core/public-api';
import { Journal } from '../../entities/public-api';
import { FilterHttpMap, FiltersService } from '../../filters/public-api';
import { Environment } from '../../core/env';
import { CatalogService } from '../../backend/public-api';
import { CatalogFiltersComponent } from '../catalog-filters/catalog-filters.component';
import { MatSnackBar, MatDialog, MatDialogRef } from '@angular/material';
export declare class CatalogComponent implements OnInit {
    private service;
    private metadata;
    private filterService;
    private env;
    private _snackBar;
    dialog: MatDialog;
    loading: boolean;
    private hasErrors;
    dataSource: MatTableDataSource<Journal>;
    columnsToDisplay: string[];
    expandedElement: Journal;
    length: number;
    pageSize: number;
    pageSizeOptions: number[];
    pageEvent: PageEvent;
    params: Array<FilterHttpMap>;
    sceibaHost: string;
    layoutPosition: {
        name: string;
        layout: string;
        aling: string;
        width: string;
    }[];
    currentlayout: {
        name: string;
        layout: string;
        aling: string;
        width: string;
    };
    constructor(service: CatalogService, metadata: MetadataService, filterService: FiltersService, env: Environment, _snackBar: MatSnackBar, dialog: MatDialog);
    paginator: MatPaginator;
    filter_component: CatalogFiltersComponent;
    ngOnInit(): void;
    fetchJournalData(): void;
    onScrollUp(): void;
    isEmpty(): boolean;
    isLoading(): boolean;
    openme(): boolean;
    changeLayoutPosition(index: number): void;
    viewJournal(uuid: string): void;
}
export declare class DialogCatalogJournalInfoDialog implements OnInit {
    dialogRef: MatDialogRef<DialogCatalogJournalInfoDialog>;
    data: any;
    constructor(dialogRef: MatDialogRef<DialogCatalogJournalInfoDialog>, data: any);
    ngOnInit(): void;
    onNoClick(): void;
}
