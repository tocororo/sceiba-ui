import { OnInit } from '@angular/core';
import { FormGroup, FormBuilder } from '@angular/forms';
import { MatDialog } from '@angular/material';
import { TaxonomyService } from '../../../backend/public-api';
import { SourceData, TermNode, Term, SourceClasification } from '../../../entities/public-api';
import { PanelContent_Depr, FormContainerAction } from '../../../forms/public-api';
export declare class SourceIndexesComponent implements OnInit {
    dialog: MatDialog;
    private service;
    sourceData: SourceData;
    editable: boolean;
    databases: TermNode[];
    selectedDatabases: Array<{
        dbclass: Term;
        dblist: SourceClasification[];
    }>;
    constructor(dialog: MatDialog, service: TaxonomyService);
    ngOnInit(): void;
    initIndexes(): void;
    /**
     * divide los source.classifications que son de INDEXES
     * en el arreglo selectedDatabases, cuyos elementos tienen
     * dbclass, que es el primer nivel
     * dblist, que son los SourceClassification seleccionados e hijos de dbclass
     */
    private _setSelectedDatabses;
    /**
     * mantiene la lista sourceData.classification bien formada en relacion con lo que hay en selectedDatabases
     */
    private _setIndexesToSource;
    addIndexAction(dbclassIndex: number): void;
    editIndexAction(dbclassIndex: number, editing: SourceClasification): void;
    deleteIndexAction(dbclassIndex: number, toDelete: SourceClasification): void;
}
export declare class SourceEditAddIndexComponent implements OnInit {
    private service;
    private _formBuilder;
    data: any;
    indexPanel: PanelContent_Depr[];
    indexFormGroup: FormGroup;
    addIndexAction: FormContainerAction;
    dbclass: Term;
    options: TermNode[];
    editing: SourceClasification;
    addIndex: any;
    constructor(service: TaxonomyService, _formBuilder: FormBuilder, data: any);
    ngOnInit(): void;
}
