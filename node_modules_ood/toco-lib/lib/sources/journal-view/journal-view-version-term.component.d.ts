import { OnInit } from '@angular/core';
import { SourceClasification } from '../../entities/public-api';
/**
 * This component share the same scss that `JournalViewComponent`.
 * His goal is show a list of `terms`
 */
export declare class JournalViewTermComponent implements OnInit {
    title: string;
    vocab_id: number;
    terms: Array<SourceClasification>;
    constructor();
    ngOnInit(): void;
}
