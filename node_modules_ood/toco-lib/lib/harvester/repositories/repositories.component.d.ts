import { OnInit } from '@angular/core';
import { MatPaginator, PageEvent } from '@angular/material/paginator';
import { MatTableDataSource } from '@angular/material/table';
import { CatalogService } from '../../backend/public-api';
import { MetadataService } from '../../core/public-api';
import { Journal } from '../../entities/public-api';
import { FilterHttpMap, FiltersService } from '../../filters/public-api';
import { Environment } from '../../core/env';
import { HarvesterFiltersComponent } from '../harvester-filters/harvester-filters.component';
export declare class RepositoriesComponent implements OnInit {
    private service;
    private metadata;
    private filterService;
    private env;
    journalList: Journal[];
    private loading;
    dataSource: MatTableDataSource<Journal>;
    columnsToDisplay: string[];
    expandedElement: Journal;
    length: number;
    pageSize: number;
    pageSizeOptions: number[];
    pageEvent: PageEvent;
    params: Array<FilterHttpMap>;
    sceibaHost: string;
    currentlayout: {
        name: string;
        layout: string;
        aling: string;
        width: string;
    };
    constructor(service: CatalogService, metadata: MetadataService, filterService: FiltersService, env: Environment);
    paginator: MatPaginator;
    filter_component: HarvesterFiltersComponent;
    ngOnInit(): void;
    fetchJournalData(): void;
    onScrollUp(): void;
    isEmpty(): boolean;
    isLoading(): boolean;
    currentJournals(): void;
}
