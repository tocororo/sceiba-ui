import { OnInit } from '@angular/core';
import { FormControl } from '@angular/forms';
import { Observable } from 'rxjs';
import { FilterComponent } from '../filter.component';
import { FiltersService } from '../filters.service';
import { FilterContainerService } from '../filter-container.service';
export declare class SelectAutocompleteFilterComponent implements OnInit, FilterComponent {
    private filterService;
    private filterContainerService;
    data: any;
    type: string;
    placeholder: string;
    text: string;
    multiple: boolean;
    selectValue: string;
    selectOptions: ValueInformation[];
    myControl: FormControl;
    filteredOptions: Observable<ValueInformation[]>;
    chipsList: ValueInformation[];
    inputId: string;
    constructor(filterService: FiltersService, filterContainerService: FilterContainerService);
    ngOnInit(): void;
    private _filter;
    remove_component(): void;
    optionSelect(): void;
    addChips(value: ValueInformation): void;
    removeChip(index: number): void;
}
export interface ValueInformation {
    id: string;
    name: string;
}
